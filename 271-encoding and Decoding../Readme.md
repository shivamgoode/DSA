Design an algorithm to encode a list of dtring to a string That encoded string is then sent into over a network and is decoded back to the original list of string.

Example:-
s=["ab, "b", "abc", "d];

Encoded string
sb={ab*b*abc*d}

Decoded String
s=["ab, "b", "abc", "d];